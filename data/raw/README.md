# Raw Data

This directory holds original, immutable raw transaction datasets. 

**Rule:** Raw data files stored here are read-only and must never be modified, overwritten, or directly altered by analytical scripts. Data payloads are excluded from version control via `.gitignore`.
