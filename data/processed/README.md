# Processed Data

This directory holds cleaned, transformed, and feature-engineered datasets generated by data pipelines and RFM processing routines. Processed data payloads are excluded from version control via `.gitignore`.
